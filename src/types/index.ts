/**
 * StickerNest v2 - Types Index
 *
 * Central export point for all type definitions
 */

// Domain types (core models)
export * from './domain';

// Runtime types (events, context)
export * from './runtime';

// Manifest types (widget manifests)
export * from './manifest';

// Capability types (I/O, permissions)
export * from './capabilities';

// Entity types (canvas entities)
export * from './entities';

// Skin types (theming)
export * from './skin';

// Widget types (unified widget contract)
export * from './widget';
